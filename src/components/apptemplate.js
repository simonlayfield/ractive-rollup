const appTemplate = `

	<p><uiSlider message="{{message}}" /></p>
	<p><uiButton label="{{label}}" /></p>
	<p>&uarr; This <em>does</em> totally work but I know it's wrong.</p>

`;

export default { appTemplate };
